sudo: required


services:
    - docker

language: bash

before_script:
    - docker-compose pull
    - docker-compose up -d
    - docker ps


script:
    - cd config/grafana/
    - ./setupLinux.sh
    - cd
    - ls
    - echo "yes !"
